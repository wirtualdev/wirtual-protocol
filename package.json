{
  "name": "github.com/wirtualdev/wirtual-protocol",
  "private": true,
  "version": "1.30.0",
  "scripts": {
    "changeset": "changeset",
    "ci:publish": "pnpm --filter @wirtual/protocol run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@wirtual/changesets-changelog-github": "^0.0.4"
  }
}
